# Local VS Code/Codespaces development uses the Vite /api proxy automatically.
# For a separate production backend, set this to the deployed API origin.
VITE_API_URL=https://YOUR-BACKEND-DOMAIN/api
VITE_USE_MOCK_API=false
VITE_RAZORPAY_KEY_ID=
